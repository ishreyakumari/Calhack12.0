## Example environment variables for local development.
## Copy this to `.env` and fill in your secrets. DO NOT commit `.env`.

GROQ_API_KEY=
GROQ_MODEL=groq-model-v1
GROQ_API_URL=https://api.groq.ai/v1/completions

# Optional image provider keys (set according to the provider you use)
IMAGE_PROVIDER=
IMAGE_API_KEY=
